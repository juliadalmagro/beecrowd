let a = Number(lines.shift());
let b = Number(lines.shift());
let c = Number(lines.shift());
let soma = (a * 2 ) + (b * 3) + (c * 5);
let media = (soma / 10).toFixed(1);

console.log(`MEDIA = ${media}`);

